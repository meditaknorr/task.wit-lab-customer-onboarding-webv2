/**
 * White list of languages that can be detected and used on the app
 * Languages detected from the uses's browser
 * >> imported on App Router Component, as is Screen Layer Router
 * >> const whitelist = ['LOWERCASE', 'LOWERCASE', 'LOWERCASE']
 * @type {string[]}
 */
export const whitelist = ['pt', 'en', 'it'];
